name: sky_error
description: Result and Failure types for Sky Core.
version: 0.0.1
resolution: workspace

environment:
  sdk: '^3.12.0'
  flutter: '3.44.0'

dependencies:
  equatable: ^2.0.8
  fpdart: ^1.2.0
dev_dependencies:
  sky_lints:
    path: ../sky_lints
