name: sky_storage
description: Base local storage abstraction package containing database interfaces and DAOs.
version: 1.0.0
repository: https://github.com/Saurabh/sky_core
resolution: workspace

environment:
  sdk: '^3.12.0'

dependencies:
  meta: ^1.10.0

dev_dependencies:
  sky_lints:
    path: ../sky_lints
