name: sky_architecture
description: Pure Dart domain layer clean architecture core contracts and utilities.
version: 1.3.0
repository: https://github.com/Saurrabhh/sky_core/tree/master/packages/sky_architecture
resolution: workspace

environment:
  sdk: '^3.12.0'

dependencies:
  equatable: ^2.0.5
  fpdart: ^1.1.0

dev_dependencies:
  sky_lints:
    path: ../sky_lints
  test: ^1.24.0
