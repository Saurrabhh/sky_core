name: sky_di
description: Dependency injection setup for Sky Core.
version: 0.0.1
resolution: workspace

environment:
  sdk: '^3.12.0'
  flutter: '3.44.0'

dependencies:
  get_it: ^9.2.1
  injectable: ^3.0.0
dev_dependencies:
  build_runner: ^2.15.0
  injectable_generator: ^3.0.2
  sky_lints:
    path: ../sky_lints
