# sky_network

Network layer for Sky Core applications, built on [dio](https://pub.dev/packages/dio).

## Usage

```dart
final client = SkyNetworkClient();
final response = await client.get('/users');
```
