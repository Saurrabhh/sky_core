name: sky_network
description: Network layer for Sky Core.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: '^3.12.0'
  flutter: '3.44.0'

dependencies:
  dio: ^5.9.2
  retrofit: ^4.9.2
  sky_error:
    path: ../sky_error
dev_dependencies:
  build_runner: ^2.15.0
  retrofit_generator: ^10.2.6
  sky_lints:
    path: ../sky_lints
