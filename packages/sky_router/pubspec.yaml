name: sky_router
description: Reusable, high-performance routing library for Sky Core wrapping go_router.
version: 1.1.5
repository: https://github.com/Saurrabhh/sky_core/tree/master/packages/sky_router
resolution: workspace

environment:
  sdk: '^3.12.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter

  go_router: ^17.3.0
  sky_telemetry: ^1.1.3
  sky_utils: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

