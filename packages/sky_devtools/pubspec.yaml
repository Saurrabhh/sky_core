name: sky_devtools
description: Draggable Picture-in-Picture DevTools menu for inspecting logs and managing runtime environments.
version: 1.1.1
repository: https://github.com/Saurrabhh/sky_core/tree/master/packages/sky_devtools
resolution: workspace

environment:
  sdk: '^3.12.0'
  flutter: '>=3.44.0'

dependencies:
  dio: ^5.4.0
  flutter:
    sdk: flutter

  path_provider: ^2.1.3
  share_plus: ^10.0.0
  sky_design_system: ^1.7.3
  sky_telemetry: ^1.1.3
  talker_bloc_logger: ^4.6.0
  talker_dio_logger: ^4.6.0
  talker_flutter: ^4.6.0

